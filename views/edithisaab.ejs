<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
/>
</head>
<body>

    <%- include('./partials/header.ejs') %>
    <div class="px-20 w-full h-screen">
        <h3 class="font-bold mb-5">Edit Hisaab of <%= khata.filename %></h3>
        <form action="/update/<%= khata._id %>" method="post">
            <input class="w-1/3 p-2 block border-2 border-zinc-200 bg-zinc-100 rounded-md mb-5 outline-none" type="text" value="<%= khata.title %>" name="title">
            <textarea class="w-1/2 h-60 p-2 block border-2 border-zinc-200 bg-zinc-100 rounded-md resize-none mb-5 outline-none" name="details"><%= khata.details %></textarea>
            <div class="flex justify-between w-1/3 text-base">
                <div>
                    <input type="checkbox" id="encrypt" class="cursor-pointer" name="encrypted" <%= khata.encrypted ? 'checked' : '' %> >
                    <label for="encrypt" class="cursor-pointer">Encrypt this file</label> <br>
                    <input class="border-2 border-zinc-200 rounded-md px-2 py-0.5 text-zinc-600 bg-zinc-100 mt-2 outline-none" type="text" placeholder="Passcode" id="passcode" name="passcode" value="<%= khata.passcode %>">
                </div>
                <div>
                    <input type="checkbox" id="shareable" class="cursor-pointer" name="shareable" <%= khata.shareable ? 'checked' : '' %> >
                    <label for="shareable" class="cursor-pointer">Shareable file</label> <br>
                    <input type="checkbox" id="editpermissions" class="mt-4 cursor-pointer" name="editable" <%=khata.editable ? 'checked' : '' %> >
                    <label for="editpermissions" class="cursor-pointer">Edit permissions</label>
                </div>
            </div>
            <input class="px-3 py-2 w-30 text-white border-2 border-blue-500 bg-blue-500 rounded-md mt-8 hover:cursor-pointer" type="submit" value="Update Hisaab">
        </form>
        </div>
    
</body>
</html>