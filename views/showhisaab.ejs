<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
    />
</head>
<body>
    <%- include('./partials/header.ejs') %>
    <div class="px-20 mb-10">
        <div class="mb-4">
            <% if (khata.encrypted) { %>
                <span class="bg-blue-500 rounded-md p-2 text-sm text-white mr-3">
                    <i class="ri-lock-line font-medium"></i> Encrypted
                </span>
            <% } else { %>
                <span class="bg-green-600 rounded-md p-2 text-sm text-white mr-3">
                    <i class="ri-check-line font-bold"></i> Available
                </span>
            <% } %>

            <% if (!khata.shareable) { %>
                <span class="bg-zinc-300 rounded-md p-2 text-sm text-black mr-4">
                    <i class="ri-eye-off-line"></i>
                </span>
            <% } else { %>
                <a href="#" id="shareLink" data-id="<%= khata._id %>" class="bg-blue-500 text-white rounded-md p-2 text-sm mr-4">
                    <i class="ri-share-line"></i> Share as a link
                </a>
            <% } %>

            <a href="/delete/<%= khata._id %>" class="bg-red-500 text-white rounded-md p-2 text-sm mr-2">Delete</a>
            <a href="/edit/<%= khata._id %>" class="bg-orange-400 rounded-md p-2 text-sm">Edit</a>
        </div>
        <div id="shareableLinkContainer" class="hidden border-2 border-zinc-300 rounded-md my-4 p-2 w-fit bg-zinc-100">
            <span class="">Your shareable link: </span>
            <a href="#" id="generatedLink" class="text-blue-500"></a>
        </div>
        <p class="text-sm text-zinc-500 font-medium">Created on <%= khata.filename %></p>
        <div class="mt-5 w-1/3">
            <h2 class="text-black text-2xl font-medium mb-2 mr-0.5"><%= khata.title %></h2>
            <p><%= khata.details %></p>
        </div>
    </div>
    <script src="/javascripts/script.js"></script>
</body>
</html>
